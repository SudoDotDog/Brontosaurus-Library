<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="shortcut icon" type="image/png" href="<%= favicon %>">
    <title><%= title %></title>
</head>

<style>
    html,
    body,
    div {
        padding: 0;
        margin: 0;
    }

    .container {
        box-sizing: border-box;
        width: 100%;
        padding: 15px;
        display: grid;
        grid-template-areas:
            "title article"
            "navigation article";
        column-gap: 5px;
        row-gap: 5px;
        grid-template-columns: auto 1fr;
        grid-template-rows: auto 1fr;
    }

    .title-area {
        grid-area: title;
        line-height: 35px;
        font-size: 30px;
        font-weight: bold;
        width: 20vw;
        max-width: 300px;
        min-width: 200px;
    }

    .navigation-area {
        grid-area: navigation;
        width: 20vw;
        max-width: 300px;
        min-width: 200px;
    }
</style>

<body>
    <div class="container">
        <div class="title-area"><%- title.replace(/ /g, '\<br>') %></div>
        <div class="navigation-area"><%- include(navigationPath, {
            trees: tree.children,
            parseLink: parseLink,
        }); %></div>
        <div style="grid-area: article"><%- article %></div>
    </div>
</body>

</html>